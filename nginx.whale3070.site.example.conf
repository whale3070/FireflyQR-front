# 与线上 /etc/nginx/sites-available/whale-vault.conf 对齐的示例片段。
# 504 on POST /api/v1/publisher/deploy-book：多为 proxy_read_timeout 短于链上确认时间。
#
# 在 location ^~ /api/v1/ 之前加入 exact match（优先级更高）：

#     location = /api/v1/publisher/deploy-book {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_connect_timeout 30s;
#         proxy_send_timeout 360s;
#         proxy_read_timeout 360s;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
